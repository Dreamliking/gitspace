fatal: pathspec 'readme.txt' did not match any files
CNN的基本网络一般包含输入层、卷积层、池化层和输出层，其中卷积层通过不同大小的卷积核，从不同的角度提出特征，经过pooling层，常用的是max-pooling操作，最后经过全连接层或者softmax层归一化成概率。
git is free software distributed under the GPL.